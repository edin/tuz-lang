// Loop examples in tuz 

fn main() -> int {
    // For loop: sum 0 to 9
    let mut sum = 0;
    
    for i = 0, 10 {
        sum = sum + i;
    }
    
    // While loop: countdown
    let mut count = 5;
    while count > 0 {
        count = count - 1;
    }
    
    // Nested loops
    let mut total = 0;
    for i = 0, 5 {
        for j = 0, 5 {
            total = total + 1;
        }
    }
    
    return total;  // Returns 25
}
